# Reuse Task: Agent Package Test Command

Fix `src/calculator.py` so the project-specific check passes. The repository has a
custom test runner in `tools/project_test.py`; generic workspace test commands are
not sufficient for this package.

When verification passes, leave the implementation fixed. Do not edit files under
`tests/`.
