# Reuse Task: Documentation Verification

Create `docs/configuration.md` for the configuration guide. Keep the docs system
consistent: every page needs required frontmatter and every page must be listed in
`docs/index.json`. Verify with the repository strict docs checker before finishing.
