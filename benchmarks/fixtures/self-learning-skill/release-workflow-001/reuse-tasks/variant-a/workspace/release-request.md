# Release Request

Create `reports/release-plan.md` for replacing the `main.js` asset on release
`1.0.0`. The plan must validate before tagging, verify the artifact exists before
uploading, and include the exact upload command for replacing the asset.
